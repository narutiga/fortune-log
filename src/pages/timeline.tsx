import { NextPage } from "next";
import { Timeline } from "src/pages-component/timeline";

const TimelinePage: NextPage = (props) => {
  return <Timeline {...props} />;
};

export default TimelinePage;
